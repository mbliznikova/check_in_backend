# Check-in

Multi-tenant attendance and class management backend for studios and schools.

Check-in helps studio and school owners run their daily operations: check students in through a kiosk terminal, track attendance, manage class schedules, pricing, and track payments — all from a single platform that supports multiple locations under one account.

The frontend (React Native + Expo, web & mobile) lives in a [separate repository](https://github.com/mbliznikova/check_in).

## Features

- **Kiosk check-in** — rate-limited check-in endpoint designed for unattended tablet terminals
- **Class scheduling** — define recurring classes with weekly schedules. Class occurrences are generated automatically
- **Attendance tracking** — per-class attendance history with student and class snapshots
- **Payment management** — per-class pricing, payment records, and monthly summaries
- **Multi-school support** — one account can manage multiple schools/studios with full data isolation
- **Staff onboarding** — owners invite users by role via email invitation links
- **Role-based access** — four-tier permission hierarchy (owner → admin → teacher → kiosk) enforced at the view level

## Tech Stack

| Layer | Technology |
|---|---|
| Framework | Django, Django REST Framework |
| Auth | Clerk (JWT/JWKS via PyJWT) |
| Task queue | Celery + Redis, Celery Beat |
| Database | PostgreSQL (production), SQLite (development) |
| Monitoring | Sentry, Mixpanel |

## Architecture

### Multi-tenant data isolation

Every model — Student, ClassModel, Payment, Attendance, and others — carries a foreign key to `School`. A custom middleware resolves the `X-School-ID` request header and attaches the school context directly to the request object (`request.school`), so all queries are automatically scoped to the correct tenant without repetitive filtering in views.

### Authentication and authorization

Clerk-issued Bearer tokens are validated on every request using PyJWKClient against Clerk's JWKS endpoint. The middleware syncs user identity idempotently in an atomic transaction, then resolves and attaches the user's school membership. View-level decorators (`@teacher_or_above`, `@admin_or_owner`, `@owner_only`, etc.) enforce the four-tier role hierarchy cleanly at the entry point.

### Background scheduling

`ClassOccurrence` records — the actual dated instances of a recurring class — are generated by a Celery Beat task that runs every Sunday at midnight. This separates the *definition* of a recurring class (stored in `ClassModel` and `Schedule`) from its *instances*, keeping the scheduling model simple and the generated records lightweight.

## Status

In active development. Production launch coming soon.
